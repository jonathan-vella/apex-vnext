# Retired Source Instructions

These files are the retired repository-source instruction set. Consumer-safe replacements live in
`customizations/.github/instructions/` and are packaged into both client projections.

The authoritative source-to-consumer disposition is
[`tools/registry/guidance-migration.v1.json`](../../tools/registry/guidance-migration.v1.json). Review the generated
[`docs/vnext/INSTRUCTION-CATALOG-REVIEW.generated.md`](../../docs/vnext/INSTRUCTION-CATALOG-REVIEW.generated.md) for
the current mapping and explicit exclusions.

Do not restore a file from this archive without updating the migration ledger, managed projection, and deterministic
validation coverage.
