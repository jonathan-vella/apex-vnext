# Manual live qualification apply for an exact locally previewed vNext candidate.
# Local APEX creates Gate 4 approval and bound authority bundles before dispatch.
name: vNext Live Qualification
run-name: vnext-live-${{ inputs.track }}-${{ inputs.operation }}-${{ inputs.handoff_id }}

on:
  workflow_dispatch:
    inputs:
      track:
        description: IaC provider track
        required: true
        type: choice
        options: [bicep, terraform]
      operation:
        description: Exact operation to qualify
        required: true
        type: choice
        options: [apply, destroy]
      handoff_id:
        description: Local authority handoff UUID
        required: true
        type: string
      candidate_sha:
        description: Exact lowercase candidate commit SHA
        required: true
        type: string
      preview_hash:
        description: Exact locally approved preview hash
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: vnext-live-${{ inputs.handoff_id }}
  cancel-in-progress: false

env:
  LEFTHOOK: "0"
  AZURE_CORE_OUTPUT: none

jobs:
  validate_dispatch:
    name: Validate exact dispatch
    permissions:
      contents: read
    runs-on: ubuntu-latest
    steps:
      - name: Checkout exact candidate
        uses: actions/checkout@v7
        with:
          ref: ${{ inputs.candidate_sha }}
          fetch-depth: 1
          persist-credentials: false
      - name: Validate immutable dispatch binding
        env:
          CANDIDATE_SHA: ${{ inputs.candidate_sha }}
          HANDOFF_ID: ${{ inputs.handoff_id }}
          PREVIEW_HASH: ${{ inputs.preview_hash }}
        run: |
          set -euo pipefail
          test "$GITHUB_REPOSITORY" = "jonathan-vella/apex-vnext"
          test "$GITHUB_REF" = "refs/heads/main"
          test "$GITHUB_RUN_ATTEMPT" = "1"
          node -e 'const [sha,handoff,preview]=process.argv.slice(1);if(!/^[0-9a-f]{40}$/.test(sha)||!/^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i.test(handoff)||!/^[0-9a-f]{64}$/.test(preview))process.exit(1)' "$CANDIDATE_SHA" "$HANDOFF_ID" "$PREVIEW_HASH"
          test "$CANDIDATE_SHA" = "$GITHUB_SHA"
          test "$CANDIDATE_SHA" = "$(git rev-parse HEAD)"

  apply:
    name: Import local Gate 4 approval and deploy exact preview
    needs: validate_dispatch
    permissions:
      contents: read
      id-token: write
    runs-on: ubuntu-latest
    environment: vnext-qualification
    timeout-minutes: 60
    env:
      APEX_CONTROL_RESOURCE_GROUP: ${{ vars.APEX_CONTROL_RESOURCE_GROUP }}
      APEX_BACKEND_STORAGE_ACCOUNT: ${{ vars.APEX_BACKEND_STORAGE_ACCOUNT }}
      APEX_BICEP_RESOURCE_GROUP: ${{ vars.APEX_BICEP_RESOURCE_GROUP }}
      APEX_TERRAFORM_RESOURCE_GROUP: ${{ vars.APEX_TERRAFORM_RESOURCE_GROUP }}
      APEX_LOG_ANALYTICS_WORKSPACE_RESOURCE_ID: ${{ vars.APEX_LOG_ANALYTICS_WORKSPACE_RESOURCE_ID }}
      APEX_LOCATION: ${{ vars.APEX_LOCATION }}
      APEX_PROJECT_NAME: ${{ vars.APEX_PROJECT_NAME }}
      APEX_QUALIFICATION_TAGS_JSON: ${{ vars.APEX_QUALIFICATION_TAGS_JSON }}
      APEX_RUNTIME_ROOT: ${{ github.workspace }}/apex-live/runtime
    steps:
      - name: Checkout exact candidate
        uses: actions/checkout@v7
        with:
          ref: ${{ inputs.candidate_sha }}
          fetch-depth: 1
          persist-credentials: false
      - name: Setup Node repository
        uses: ./.github/actions/setup-node-repo
      - name: Build vNext
        run: npm run build:vnext
      - name: Azure OIDC login
        uses: azure/login@v3
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v4
        with:
          terraform_version: 1.15.8
          terraform_wrapper: false
      - name: Acquire ARM OIDC token
        id: arm_oidc
        uses: actions/github-script@v9
        with:
          result-encoding: string
          script: |
            const token = await core.getIDToken("api://AzureADTokenExchange");
            core.setSecret(token);
            return token;
      - name: Validate protected inputs
        env:
          AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
        run: |
          set -euo pipefail
          for name in APEX_CONTROL_RESOURCE_GROUP APEX_BACKEND_STORAGE_ACCOUNT APEX_BICEP_RESOURCE_GROUP APEX_TERRAFORM_RESOURCE_GROUP APEX_LOG_ANALYTICS_WORKSPACE_RESOURCE_ID APEX_LOCATION APEX_PROJECT_NAME APEX_QUALIFICATION_TAGS_JSON; do test -n "${!name:-}"; done
          node tools/scripts/validate-vnext-qualification-context.mjs
          test "$(az account show --query id --output tsv)" = "$AZURE_SUBSCRIPTION_ID"
          test "$GITHUB_SHA" = "$(git rev-parse HEAD)"
      - name: Define stable authority recipient
        id: names
        env:
          HANDOFF_ID: ${{ inputs.handoff_id }}
        run: |
          set -euo pipefail
          apply_recipient="github-actions:${GITHUB_REPOSITORY}:handoff:${HANDOFF_ID}:apply"
          printf 'apply_recipient=%s\n' "$apply_recipient" >> "$GITHUB_OUTPUT"
      - name: Validate at-rest endpoint boundary
        run: |
          set -euo pipefail
          state="$(az storage account show --resource-group "$APEX_CONTROL_RESOURCE_GROUP" --name "$APEX_BACKEND_STORAGE_ACCOUNT" --query '{publicNetworkAccess:publicNetworkAccess,defaultAction:networkRuleSet.defaultAction,ipRules:networkRuleSet.ipRules,securityControl:tags.SecurityControl,allowSharedKeyAccess:allowSharedKeyAccess,allowBlobPublicAccess:allowBlobPublicAccess,defaultToOAuthAuthentication:defaultToOAuthAuthentication}' --output json)"
          STATE="$state" node -e 'const value=JSON.parse(process.env.STATE);if(value.publicNetworkAccess!=="Disabled"||value.defaultAction!=="Deny"||(value.ipRules??[]).length!==0||value.securityControl!=null||value.allowSharedKeyAccess!==false||value.allowBlobPublicAccess!==false||value.defaultToOAuthAuthentication!==true)process.exit(1)'
      - name: Open temporary Entra-only endpoint session
        run: |
          set -euo pipefail
          node tools/scripts/validate-vnext-qualification-context.mjs --security-exception-only
          az storage account update --resource-group "$APEX_CONTROL_RESOURCE_GROUP" --name "$APEX_BACKEND_STORAGE_ACCOUNT" --set tags.SecurityControl=Ignore --only-show-errors --output none
          az storage account update --resource-group "$APEX_CONTROL_RESOURCE_GROUP" --name "$APEX_BACKEND_STORAGE_ACCOUNT" --public-network-access Enabled --only-show-errors --output none
          az storage account update --resource-group "$APEX_CONTROL_RESOURCE_GROUP" --name "$APEX_BACKEND_STORAGE_ACCOUNT" --default-action Allow --only-show-errors --output none
          for attempt in 1 2 3 4 5; do
            if az storage blob list --auth-mode login --account-name "$APEX_BACKEND_STORAGE_ACCOUNT" --container-name handoff --num-results 1 --only-show-errors --output none; then exit 0; fi
            test "$attempt" -lt 5
            node -e 'setTimeout(()=>{},Number(process.argv[1]))' "$((attempt * 5000))"
          done
          exit 1
      - name: Download bound local authority
        run: |
          set -euo pipefail
          mkdir -p apex-live/evidence apex-live/incoming apex-live/plans "$APEX_RUNTIME_ROOT"
          for object in state provider; do
            for attempt in 1 2 3 4 5; do
              if az storage blob download --auth-mode login --account-name "$APEX_BACKEND_STORAGE_ACCOUNT" --container-name handoff --name "incoming/${{ inputs.handoff_id }}/${object}.json" --file "apex-live/incoming/${object}.json" --overwrite true --only-show-errors --output none; then break; fi
              test "$attempt" -lt 5
              node -e 'setTimeout(()=>{},Number(process.argv[1]))' "$((attempt * 5000))"
            done
          done
      - name: Import authority and validate local approval
        id: accept
        env:
          APPLY_RECIPIENT: ${{ steps.names.outputs.apply_recipient }}
          PREVIEW_HASH: ${{ inputs.preview_hash }}
          TRACK: ${{ inputs.track }}
        run: |
          set -euo pipefail
          cli="$GITHUB_WORKSPACE/packages/cli/dist/cli.js"
          evidence="$GITHUB_WORKSPACE/apex-live/evidence"
          incoming="$GITHUB_WORKSPACE/apex-live/incoming"
          cd "$APEX_RUNTIME_ROOT"
          node "$cli" state transfer-import --file "$incoming/state.json" --recipient "$APPLY_RECIPIENT" --yes --json > "$evidence/state-import.json"
          claim_hash="$(jq -er '.result.claimHash' "$evidence/state-import.json")"
          node "$cli" provider transfer-import --file "$incoming/provider.json" --recipient "$APPLY_RECIPIENT" --yes --json > "$evidence/provider-import.json"
          test "$(jq -er '.result.previewHash' "$evidence/provider-import.json")" = "$PREVIEW_HASH"
          test "$(jq -er '.result.provider' "$evidence/provider-import.json")" = "$TRACK"
          node "$cli" writer transfer-accept --claim "$claim_hash" --recipient "$APPLY_RECIPIENT" --head "$GITHUB_SHA" --json > "$evidence/writer-accept.json"
          printf 'accepted=true\n' >> "$GITHUB_OUTPUT"
          node "$cli" approval show --json > "$evidence/approval.json"
          node "$cli" status --json > "$evidence/status.json"
          APPROVAL="$evidence/approval.json" STATUS="$evidence/status.json" node -e 'const fs=require("node:fs");const approval=JSON.parse(fs.readFileSync(process.env.APPROVAL)).result;const run=JSON.parse(fs.readFileSync(process.env.STATUS)).result.run;const expected={gate:4,decision:"approved",mechanism:"tty",previewHash:process.env.PREVIEW_HASH,recipientIdentity:process.env.APPLY_RECIPIENT};for(const [key,value] of Object.entries(expected))if(approval?.[key]!==value)process.exit(1);if(!Number.isInteger(approval?.writerEpoch)||run?.ownerEpoch!==approval.writerEpoch+1)process.exit(1)'
          test "$(jq -er '.result.run.iacTool' "$evidence/status.json")" = "$TRACK"
          test "$GITHUB_SHA" = "$(git rev-parse HEAD)"
          for object in state provider; do
            az storage blob delete --auth-mode login --account-name "$APEX_BACKEND_STORAGE_ACCOUNT" --container-name handoff --name "incoming/${{ inputs.handoff_id }}/${object}.json" --only-show-errors --output none
          done
      - name: Generate deterministic provider configuration
        env:
          ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          ARM_OIDC_TOKEN: ${{ steps.arm_oidc.outputs.result }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          ARM_USE_AZUREAD: "true"
          ARM_USE_OIDC: "true"
          PREVIEW_HASH: ${{ inputs.preview_hash }}
          TRACK: ${{ inputs.track }}
        run: |
          set -euo pipefail
          if test "$TRACK" = bicep; then
            jq -n --arg project "$APEX_PROJECT_NAME" --arg location "$APEX_LOCATION" --arg workspace "$APEX_LOG_ANALYTICS_WORKSPACE_RESOURCE_ID" --argjson tags "$APEX_QUALIFICATION_TAGS_JSON" '{"$schema":"https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",contentVersion:"1.0.0.0",parameters:{projectName:{value:$project},environment:{value:"qualification"},location:{value:$location},tags:{value:$tags},logAnalyticsWorkspaceResourceId:{value:$workspace}}}' > apex-live/main.parameters.json
            jq -n --arg rg "$APEX_BICEP_RESOURCE_GROUP" --arg template "$GITHUB_WORKSPACE/infra/bicep/vnext-qualification/main.bicep" --arg params "$GITHUB_WORKSPACE/apex-live/main.parameters.json" '{bicep:{resourceGroup:$rg,deploymentName:"vnext-qualification",stackName:"vnext-qualification",templateFile:$template,parametersFile:$params,actionOnUnmanage:"deleteResources",ownershipAuthorizesDeleteResources:true,dedicatedSandboxResourceGroup:true,denySettingsMode:"none"}}' > apex-live/provider-config.json
          else
            printf 'resource_group_name = "%s"\nstorage_account_name = "%s"\ncontainer_name = "tfstate"\nkey = "vnext-qualification.tfstate"\nuse_oidc = true\nuse_azuread_auth = true\n' "$APEX_CONTROL_RESOURCE_GROUP" "$APEX_BACKEND_STORAGE_ACCOUNT" > apex-live/backend.hcl
            printf 'TF_CLI_ARGS_init=-backend-config=%s\n' "$GITHUB_WORKSPACE/apex-live/backend.hcl" >> "$GITHUB_ENV"
            printf 'TF_VAR_project_name=%s\nTF_VAR_environment=qualification\nTF_VAR_location=%s\nTF_VAR_resource_group_name=%s\nTF_VAR_log_analytics_workspace_resource_id=%s\nTF_VAR_tags=%s\n' "$APEX_PROJECT_NAME" "$APEX_LOCATION" "$APEX_TERRAFORM_RESOURCE_GROUP" "$APEX_LOG_ANALYTICS_WORKSPACE_RESOURCE_ID" "$APEX_QUALIFICATION_TAGS_JSON" >> "$GITHUB_ENV"
            export TF_VAR_project_name="$APEX_PROJECT_NAME" TF_VAR_environment=qualification TF_VAR_location="$APEX_LOCATION" TF_VAR_resource_group_name="$APEX_TERRAFORM_RESOURCE_GROUP" TF_VAR_log_analytics_workspace_resource_id="$APEX_LOG_ANALYTICS_WORKSPACE_RESOURCE_ID" TF_VAR_tags="$APEX_QUALIFICATION_TAGS_JSON"
            lock_hash="$(sha256sum infra/terraform/vnext-qualification/.terraform.lock.hcl | cut -d ' ' -f 1)"
            attested_lock_hash="$(jq -er '.attestation.lockfileHash' "$APEX_RUNTIME_ROOT/.apex/local/provider-runtime/bindings/${PREVIEW_HASH}.json")"
            test "$lock_hash" = "$attested_lock_hash"
            terraform -chdir=infra/terraform/vnext-qualification init -input=false -backend-config="$GITHUB_WORKSPACE/apex-live/backend.hcl"
            test "$(sha256sum infra/terraform/vnext-qualification/.terraform.lock.hcl | cut -d ' ' -f 1)" = "$attested_lock_hash"
            jq -n --arg cwd "$GITHUB_WORKSPACE/infra/terraform/vnext-qualification" --arg target "$APEX_TERRAFORM_RESOURCE_GROUP" --arg plans "$GITHUB_WORKSPACE/apex-live/plans" --arg lock "$lock_hash" '{terraform:{cwd:$cwd,target:$target,planDirectory:$plans,lockfileHash:$lock}}' > apex-live/provider-config.json
          fi
      - name: Refresh ARM OIDC token for deploy
        id: deploy_oidc
        uses: actions/github-script@v9
        with:
          result-encoding: string
          script: |
            const token = await core.getIDToken("api://AzureADTokenExchange");
            core.setSecret(token);
            return token;
      - name: Deploy exact preview
        env:
          ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          ARM_OIDC_TOKEN: ${{ steps.deploy_oidc.outputs.result }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          ARM_USE_AZUREAD: "true"
          ARM_USE_OIDC: "true"
        run: |
          set -euo pipefail
          cli="$GITHUB_WORKSPACE/packages/cli/dist/cli.js"
          evidence="$GITHUB_WORKSPACE/apex-live/evidence"
          cd "$APEX_RUNTIME_ROOT"
          node "$cli" deploy --preview "${{ inputs.preview_hash }}" --provider-config "$GITHUB_WORKSPACE/apex-live/provider-config.json" --json > "$evidence/deploy.json"
          node "$cli" render --kind approval --json > "$evidence/approval-render.json" || true
          node "$cli" inventory --json > "$evidence/inventory.json" || true
          node "$cli" diagnose --json > "$evidence/diagnose.json" || true
      - name: Return authority to local maintainer
        id: return
        if: always() && steps.accept.outputs.accepted == 'true'
        env:
          APPLY_RECIPIENT: ${{ steps.names.outputs.apply_recipient }}
          LOCAL_RECIPIENT: local:${{ inputs.handoff_id }}
        run: |
          set -euo pipefail
          cli="$GITHUB_WORKSPACE/packages/cli/dist/cli.js"
          evidence="$GITHUB_WORKSPACE/apex-live/evidence"
          cd "$APEX_RUNTIME_ROOT"
          node "$cli" writer transfer-create --repo "$GITHUB_REPOSITORY" --branch "$GITHUB_REF_NAME" --commit "$GITHUB_SHA" --workflow "$GITHUB_WORKFLOW_REF" --sender "$APPLY_RECIPIENT" --recipient "$LOCAL_RECIPIENT" --head "$GITHUB_SHA" --ttl 3600000 --json > "$evidence/return-claim.json"
          claim_hash="$(jq -er '.result.hash' "$evidence/return-claim.json")"
          node "$cli" state transfer-export --claim "$claim_hash" --recipient "$LOCAL_RECIPIENT" --ttl-seconds 3000 --file "$GITHUB_WORKSPACE/apex-live/return-authority.json" --yes --json > "$evidence/return-export.json"
          blob="return/${{ inputs.handoff_id }}/${{ inputs.track }}-${{ inputs.operation }}.json"
          for attempt in 1 2 3 4 5; do
            if az storage blob upload --auth-mode login --account-name "$APEX_BACKEND_STORAGE_ACCOUNT" --container-name handoff --name "$blob" --file "$GITHUB_WORKSPACE/apex-live/return-authority.json" --overwrite false --only-show-errors --output none; then break; fi
            test "$attempt" -lt 5
            node -e 'setTimeout(()=>{},Number(process.argv[1]))' "$((attempt * 5000))"
          done
          printf 'claim_hash=%s\nblob=%s\n' "$claim_hash" "$blob" >> "$GITHUB_OUTPUT"
      - name: Upload bound return fallback
        if: always() && steps.accept.outputs.accepted == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: vnext-return-${{ inputs.handoff_id }}-${{ inputs.track }}-${{ inputs.operation }}-apply
          path: apex-live/return-authority.json
          retention-days: 1
          compression-level: 0
          include-hidden-files: false
          if-no-files-found: error
      - name: Close temporary Entra-only endpoint session
        if: always()
        run: |
          set +e
          az storage account update --resource-group "$APEX_CONTROL_RESOURCE_GROUP" --name "$APEX_BACKEND_STORAGE_ACCOUNT" --default-action Deny --only-show-errors --output none
          deny_status=$?
          az storage account update --resource-group "$APEX_CONTROL_RESOURCE_GROUP" --name "$APEX_BACKEND_STORAGE_ACCOUNT" --public-network-access Disabled --only-show-errors --output none
          disable_status=$?
          az storage account update --resource-group "$APEX_CONTROL_RESOURCE_GROUP" --name "$APEX_BACKEND_STORAGE_ACCOUNT" --remove tags.SecurityControl --only-show-errors --output none
          exclusion_status=$?
          endpoint_state="$(az storage account show --resource-group "$APEX_CONTROL_RESOURCE_GROUP" --name "$APEX_BACKEND_STORAGE_ACCOUNT" --query '{publicNetworkAccess:publicNetworkAccess,defaultAction:networkRuleSet.defaultAction,securityControl:tags.SecurityControl}' --output json 2>/dev/null)"
          verify_status=$?
          public_access="$(jq -r '.publicNetworkAccess // ""' <<<"$endpoint_state" 2>/dev/null)"
          default_action="$(jq -r '.defaultAction // ""' <<<"$endpoint_state" 2>/dev/null)"
          security_control="$(jq -r '.securityControl // ""' <<<"$endpoint_state" 2>/dev/null)"
          if [[ "$public_access" != "Disabled" || "$default_action" != "Deny" || -n "$security_control" ]]; then verify_status=1; fi
          status=0
          if [[ "$deny_status" -ne 0 || "$disable_status" -ne 0 || "$exclusion_status" -ne 0 || "$verify_status" -ne 0 ]]; then status=1; fi
          mkdir -p apex-live/evidence
          jq -n --arg job apply --arg session "entra-only-public-endpoint" --arg publicAccess "$public_access" --arg defaultAction "$default_action" --arg securityControl "$security_control" --argjson restored "$( [[ "$deny_status" -eq 0 && "$disable_status" -eq 0 && "$exclusion_status" -eq 0 && "$verify_status" -eq 0 ]] && printf true || printf false )" '{job:$job,session:$session,restored:$restored,publicNetworkAccess:$publicAccess,defaultAction:$defaultAction,securityControl:$securityControl}' > apex-live/evidence/cleanup.json
          exit "$status"
      - name: Upload apply evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: vnext-apply-evidence-${{ inputs.handoff_id }}-${{ inputs.track }}-${{ inputs.operation }}
          path: apex-live/evidence/
          retention-days: 1
          compression-level: 0
          include-hidden-files: false
          if-no-files-found: error
